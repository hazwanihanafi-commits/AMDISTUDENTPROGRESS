<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>AMDI Login</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="/css/student.css">
  <style>
    .login-container {
      max-width: 420px;
      margin: 80px auto;
      padding: 28px;
      background: white;
      border-radius: 14px;
      box-shadow: var(--soft-shadow);
    }
    .login-title {
      font-size: 24px;
      font-weight: 800;
      color: var(--navy);
      text-align: center;
      margin-bottom: 4px;
    }
    .login-sub {
      text-align: center;
      color: #556;
      margin-bottom: 26px;
      font-size: 14px;
    }
    .login-input {
      width: 100%;
      padding: 12px;
      border-radius: 10px;
      border: 1px solid #dfe6ef;
      margin: 8px 0 16px 0;
      font-size: 15px;
    }
    .login-btn {
      width: 100%;
      padding: 12px 16px;
      background: var(--navy);
      color: white;
      border: none;
      border-radius: 10px;
      font-size: 16px;
      font-weight: 700;
      cursor: pointer;
    }
    .login-btn:hover {
      opacity: 0.9;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="login-container">
      <div class="login-title">AMDI Student Progress</div>
      <div class="login-sub">Supervisor Login</div>

      <form action="/login" method="post">
        <input type="email" name="email" class="login-input" placeholder="Email" required>
        <input type="password" name="password" class="login-input" placeholder="Password" required>
        <button type="submit" class="login-btn">Login</button>
      </form>
    </div>
  </div>
</body>
</html>
